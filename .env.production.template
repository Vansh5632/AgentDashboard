# Production Environment Variables Template
# Copy this to your Railway/Vercel environment variables

# ========================================
# API Service (Railway)
# ========================================

NODE_ENV=production
PORT=3001

# Database (Railway PostgreSQL)
DATABASE_URL=postgresql://postgres:PASSWORD@HOST:PORT/DBNAME?sslmode=require

# Redis (Upstash or Railway)
REDIS_HOST=your-redis-host.upstash.io
REDIS_PORT=6379
# REDIS_PASSWORD=<if using Upstash with TLS>

# Authentication & Security
JWT_SECRET=<generate with: openssl rand -base64 32>
ENCRYPTION_KEY=<generate with: openssl rand -hex 16>

# ElevenLabs Voice AI
ELEVENLABS_API_KEY=sk_xxxxxxxxxxxxxxxxxxxxx

# CORS (comma-separated, update after Vercel deployment)
CORS_ORIGINS=https://your-app.vercel.app,https://your-custom-domain.com

# Optional Integrations
CALCOM_API_KEY=cal_live_xxxxxxxxxxxxx
GHL_API_KEY=ghl_xxxxxxxxxxxxxxxxxxxxx

# ========================================
# Worker Service (Railway)
# ========================================

NODE_ENV=production

# Database (same as API)
DATABASE_URL=postgresql://postgres:PASSWORD@HOST:PORT/DBNAME?sslmode=require

# Redis (same as API)
REDIS_HOST=your-redis-host.upstash.io
REDIS_PORT=6379

# OpenAI for AI Summaries
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxx

# Pinecone for Vector Search
PINECONE_API_KEY=pcsk_xxxxxxxxxxxxxxxxxxxxx
PINECONE_INDEX_NAME=ai-agent-calls

# ========================================
# Frontend (Vercel)
# ========================================

# API URL (Railway API service public URL)
NEXT_PUBLIC_API_URL=https://your-api.up.railway.app

# ========================================
# Notes
# ========================================

# 1. Generate JWT_SECRET: openssl rand -base64 32
# 2. Generate ENCRYPTION_KEY: openssl rand -hex 16 (must be 32 chars)
# 3. Update CORS_ORIGINS after deploying to Vercel
# 4. Use same DATABASE_URL and REDIS credentials for both API and Worker
# 5. Railway allows referencing other services: ${{Postgres.DATABASE_URL}}
# 6. For Upstash Redis with TLS, use the TLS endpoint and add REDIS_PASSWORD
