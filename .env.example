# ==============================================
# RAILWAY DEPLOYMENT - Environment Variables
# ==============================================
# Copy this file to .env for local development
# For Railway: Set these in each service's environment variables
# Railway will auto-inject DATABASE_URL, REDIS_HOST, REDIS_PORT when you link services

# ==============================================
# DATABASE (Railway PostgreSQL Service)
# ==============================================
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# ==============================================
# REDIS (Railway Redis Service)
# ==============================================
REDIS_HOST="localhost"
REDIS_PORT="6379"
# Optional: Railway Redis password (if enabled)
REDIS_PASSWORD=""
REDIS_URL="redis://localhost:6379"

# ==============================================
# API SERVICE CONFIGURATION
# ==============================================
NODE_ENV="production"
PORT=3001

# JWT Authentication - CHANGE THIS!
# Generate with: openssl rand -base64 32
JWT_SECRET="your_jwt_secret_for_authentication"

# Encryption Key for storing credentials - CHANGE THIS!
# Generate with: openssl rand -hex 16
ENCRYPTION_KEY="your_32_character_encryption_key_here"

# CORS Origins (comma-separated) - UPDATE WITH YOUR DOMAINS!
CORS_ORIGINS="https://yourdomain.com,https://app.yourdomain.com"

# ==============================================
# WORKER SERVICE - AI & EXTERNAL APIS
# ==============================================

# OpenAI API Key (required for call summaries)
OPENAI_API_KEY="sk-..."

# Pinecone Vector Database (required for embeddings)
PINECONE_API_KEY="your_pinecone_api_key"
PINECONE_INDEX_NAME="ai-agent-calls"

# ElevenLabs API Key (required for voice AI)
ELEVENLABS_API_KEY="your_elevenlabs_api_key"

# ==============================================
# WEB/FRONTEND SERVICE
# ==============================================

# Public API URL - Point to your Railway API service
# Example: https://ai-agent-api.railway.app
NEXT_PUBLIC_API_URL="http://localhost:3001"

# ==============================================
# EXTERNAL SERVICES (stored in DB, optional here)
# ==============================================

# Cal.com API (for meeting scheduling)
CALCOM_API_BASE_URL="https://api.cal.com/v1"
CALCOM_API_KEY="your_calcom_api_key"

# GoHighLevel (for WhatsApp notifications)
GHL_API_KEY="your_ghl_api_key"

# ==============================================
# RAILWAY DEPLOYMENT NOTES
# ==============================================
# 1. Create PostgreSQL service first
# 2. Create Redis service second
# 3. Create API, Worker, and Web services
# 4. Link PostgreSQL and Redis to each service using Railway's service linking
# 5. Set service-specific variables in Railway dashboard
# 6. Use "Reference Variables" to share DATABASE_URL across services
