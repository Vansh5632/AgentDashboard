[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/worker/Dockerfile"
dockerContext = "./"

[deploy]
# CMD in Dockerfile handles database wait and worker startup automatically
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10