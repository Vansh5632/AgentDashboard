[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"
dockerContext = "./"

[deploy]
# Dockerfile ENTRYPOINT handles migrations and server startup automatically
# No need for startCommand - Docker ENTRYPOINT will be used
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
